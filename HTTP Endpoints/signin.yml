info:
  name: signin
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/auth/signin
  body:
    type: json
    data: |-
      {
        "username": "EmployeeWithPopulatedSchedule1",
        "password": "P@ssW0rd@8675309"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
