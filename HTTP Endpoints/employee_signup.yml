info:
  name: employee_signup
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/auth/employee_signup
  body:
    type: json
    data: |-
      {
        "username": "name",
        "password": "oih*(YAD*(AYn98yda98ydna"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
