info:
  name: signup
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/auth/employer_signup
  body:
    type: json
    data: |-
      {
        "username": "EmployerWithPopulatedSchedule1",
        "password": "P@ssW0rd@8675309",
        "email": "jakeclouseclash1+employer@gmail.com",
        "compensation_rate":"1000000.0"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
